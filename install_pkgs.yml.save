---

- hosts: Ubuntu_servers
  become: true
  tasks:

  - name: update the repo and install apache package
    package:
        name: 
           - "{{ apache_package }}"
           -  "{{ stress-ng_package }}"
        state: latest
        update_cache: yes

- hosts: Ubuntu_servers
  become: true
  tasks:

 7   - name: update the repo and install apache package
 8     package:
 9         name:
10            - "{{ apache_package }}"
11            -  "{{ stress-ng_package }}"
12         state: latest
13         update_cache: yes

